<form method="post" action="{{ path('cv_delete', {'id': cv.id}) }}" onsubmit="return confirm('voulez-vous vraiment le supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cv.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
